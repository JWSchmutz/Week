<div class="row">
    <div class="col-12">
        <nav class="navbar navbar-expand-lg navbar">
            <a class="navbar-brand" href="/"><img src="/assets/images/StrongLogo2.png" alt="strong logo"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"><i class="fas fa-angle-double-down"></i></span>

            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/aboutus">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/whyHDX">Why use HDX?</a>
                    </li>
                    {{#if username}}
                    <li class="nav-item">
                        <a class="nav-link" href="/product">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">My Dashboard</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
            <div id="right">
                <ul class="navbar-nav">
                    {{#unless username}}
                    <li class="nav-item">
                        <a class="nav-link" href="/users/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/register">Register</a>
                    </li>
                    {{/unless}}
                    {{#if username}}
                    <li class="nav-item">
                        <div class="nav-link">Hello, {{username}}</div>
                    </li>
                    <li>
                        <a class="nav-link" href="/users/logout">Logout</a>
                    </li>
                    {{/if}}
                </ul>              
            </div>
        </nav>
    </div>
</div>
<div id="loginPg">
    <div class="row">
        <div class="col-md-6 m-auto">
            <div class="card card-body">
                <h1 class="text-center mb-3"><i class="fas fa-sign-in-alt"></i> Login</h1>
                {{!-- <% include ./partials/messages %> --}}
                <form action="/users/login" method="POST">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" class="form-control" placeholder="Enter Email" />
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" class="form-control"
                            placeholder="Enter Password" />
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Login</button>
                </form>
                <div style="text-align: center" class="lead mt-4">
                   <button class="btn btn-primary">Forgot password?</button> 
                   <button class="btn btn-primary">Forgot username?</button> 
                </div>
                <p style="text-align: center"class="lead mt-4">
                    No Account? <a href="/users/register">Register</a>
                </p>
            </div>
        </div>
        <div class="col-md-6 m-auto">
            <div id="loginSubtextBox" class="card card-body">
                <p>Ensure you have the lowest prices for all your critical medical supplies.</p>
            </div>
        </div>
    </div>
</div>