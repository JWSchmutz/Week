<div class="row">
    <div class="col-12">
        <nav class="navbar navbar-expand-lg navbar">
            <a class="navbar-brand" href="#"><img src="/content/images/StrongLogo2.png" alt="strong logo"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"><i class="fas fa-angle-double-down"></i></span>

            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/aboutus">About Us <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/whyHDX">Why use HDX?</a>
                    </li>
                    {{#unless username}}
                    <li class="nav-item active">
                        <a class="nav-link disabled" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link disabled" href="/products">Products</a>
                    </li>
                    {{/unless}}

                    {{#if username}}
                    <li class="nav-item active">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/product">Products</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
            <div id="right">
                <form class="form-inline my-2 my-lg-0">
                    {{#unless username}}
                    <a class="nav-link" href="/users/login">Login</a>
                    <a class="nav-link" href="/users/register">Register</a>
                    {{/unless}}
                    {{#if username}}
                    <a class="nav-link" href="/dashboard">Welcome, {{username}}!</a>
                    <a class="nav-link" href="/users/logout">Logout</a>
                    {{/if}}

                </form>
            </div>
        </nav>
    </div>
</div>
<div id="landingPg">
    <!-- Button trigger modal -->
    {{!-- <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#landingPgModal">
        Demo Modal
    </button> --}}

    <!-- Modal -->
    <div class="modal fade" id="landingPgModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
        aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalCenterTitle">Honest Data Exchange</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Welcome to Honest Data Exchange!</p>
                    <p>Your source for the best wholesale pricing information on the products you buy.</p>
                    <h2>Are you paying too much?</h2>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Find out more.</button>
                    {{!-- <button type="button" class="btn btn-primary">Save changes</button> --}}
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 m-auto">
            <div class="card card-body text-center">
                <h1><i class="fas fa-notes-medical"></i></h1>
                <p>Create an account or login</p>
                <a href="/users/register" class="btn btn-primary btn-block mb-2">Register</a>
                <a href="/users/login" class="btn btn-secondary btn-block">Login</a>
            </div>
        </div>
    </div>
</div>